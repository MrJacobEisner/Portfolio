import React from "react";
import "./Title.scss";

function Title(props: any) {
    return (
        <div className="Title">
            <h1>Let's get the</h1>
            <h1>
                ball rolling<span>.</span>
            </h1>
        </div>
    );
}

export default Title;
